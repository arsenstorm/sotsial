name: sotsial

services:
  postgres:
    image: postgres:17.7
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sotsial
    ports:
      - "6432:5432"
    volumes:
      - sotsial-data:/var/lib/postgresql/data

volumes:
  sotsial-data:
